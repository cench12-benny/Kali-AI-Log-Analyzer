Based on the network log capture data, I can provide the following analysis:

**Active Interfaces:**

1. eth0: This interface is active with an IP address of 10.0.2.15. 
2. eth1: This interface is active with an IP address of 192.168.1.50.
3. wlan0: Although this interface is listed as DOWN, it's worth checking the status to confirm. If it is indeed down, consider investigating why it's not active.
4. ext0 (with IP 185.220.101.5): This interface is active with an external IP address.

**Security Anomalies:**

1. **Duplicate Entry:** There is a duplicate entry for eth0 (same interface name and IP address). This might be a logging error or a system inconsistency, and it's worth investigating further to ensure there are no connectivity issues.
2. **Interface Name 'ext0':** It's not conventional to use 'ext0' as an interface name; typically, interface names are more descriptive, such as 'ethernet0' or 'wlan'.
3. **EXTERNAL IP (185.220.101.5):** The external interface is accessible from the internet, which might be a concern from a security perspective. Ensure this interface is properly secured and only accessible by authorized users or services.

**Suggestions for Next Steps:**

To investigate these issues further, I recommend running the following terminal commands:

1. **Check interface status and logs:** Run `ip link show` and `journalctl -u systemd-networkd` to confirm the interface status and check for any relevant logs.
2. **Verify connection issues:** Run `ping` or `traceroute` to diagnose connectivity issues with the external interface.
3. **Check network configurations:** Run `netstat -rn` to show the routing table and ensure there are no unusual routing configurations.
4. **Check firewall and security rules:** Run `iptables -n -L` or `ufw status` to check firewall rules and ensure that the external interface is properly secured.
5. **Analyze system logs:** Run `journalctl` to look for any security-related events or errors that might indicate malicious activity.

These steps will help identify the root cause of the security anomalies mentioned above and ensure that your network is properly secured.